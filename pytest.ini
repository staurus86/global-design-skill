[pytest]
; Root suites only. mcp-server has its own `tests` package (name collision) —
; run it separately: cd mcp-server && python -m pytest tests/ -q
testpaths = tests sedi/tests
norecursedirs = mcp-server .git node_modules .playwright-mcp
